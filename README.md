The component is named TaktikalSearch
I felt the need to create a child component => ResultItem
I hope that's okay.

For styling I used Styled Components and CSS variables.

When an item is selected, it logs to the console.